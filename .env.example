# Convex deployment URL - Required for both development and production
# Development will use the URL from convex dev command
# Production needs this URL to be set explicitly
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Optional: Custom domain URL if you're using Convex Pro with a custom domain
# VITE_CONVEX_URL=https://api.yourdomain.com

# Convex deploy key - Only needed for CI/CD environments to deploy Convex functions
# CONVEX_DEPLOY_KEY=your_deploy_key_here 